using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class CatchBoxBehavior : MonoBehaviour
{
    // public GameObject scaledItsuPrefab;

    // Start is called before the first frame update
    void Start()
    {
        // Instantiate(scaledItsuPrefab, new Vector3(-19.587f, -159.3f, -200.96f), Quaternion.identity);
    }

    void OnTriggerEnter(Collider other)
    {
//        if (other.gameObject.tag == "Language Ball")
  //      {
    //        StartCoroutine("RestartChallenge");
      //  }
    }

//    IEnumerator RestartChallenge()
  //  {
    //    yield return new WaitForSeconds(15f); 
      //  SceneManager.LoadScene(SceneManager.GetActiveScene().buildIndex);
    //}

}
